eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 9000

spring:
  application:
    name: proxy-client

springdoc:
  packages-to-scan:
    com.trvankiet.app
  api-docs:
    path: /v3/api-docs/proxy-client

#management:
#  tracing:
#    sampling:
#      probability: 1.0
