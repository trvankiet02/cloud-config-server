eureka:
  instance:
    hostname: service-discovery
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://service-discovery:8761/eureka/

server:
  port: 9000

api:
  prefix: api
  version: v1

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: websocket_route
          uri: lb:ws://conversation-service
          predicates:
            - Path=/ws/**
          filters:
            - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials

      globalcors:
        add-to-simple-url-handler-mapping: true
        cors-configurations:
          "[/**]":
            allow-credentials: true
            allowed-origins:
              - "http://localhost:3000"
            allowed-headers:
              - "*"
            allowed-methods:
              - HEAD
              - OPTIONS
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH

      discovery:
        locator:
          enabled: true

springdoc:
  swagger-ui:
    urls:
      - name: conversation-service
        url: http://conversation-service:8100/v3/api-docs/conversation-service
      - name: friend-service
        url: http://friend-service:8200/v3/api-docs/friend-service
      - name: group-service
        url: http://group-service:8300/v3/api-docs/group-service
      - name: media-service
        url: http://media-service:8400/v3/api-docs/media-service
      - name: post-service
        url: http://post-service:8700/v3/api-docs/post-service
      - name: user-service
        url: http://user-service:8000/v3/api-docs/user-service
      - name: exam-service
        url: http://exam-service:8800/v3/api-docs/exam-serDvice

resilience4j:
  circuitbreaker:
    instances:
      apiGateway:
        register-health-indicator: true
        event-consumer-buffer-size: 10
        automatic-transition-from-open-to-half-open-enabled: true
        failure-rate-threshold: 50
        minimum-number-of-calls: 5
        permitted-number-of-calls-in-half-open-state: 3
        sliding-window-size: 10
        wait-duration-in-open-state: 5s
        sliding-window-type: COUNT_BASED

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    circuitbreakers:
      enabled: true
  endpoint:
    health:
      show-details: always
# management:
#  tracing:
#    sampling:
#      probability: 1.0
